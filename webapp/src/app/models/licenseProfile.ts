export interface LicenseProfile {
    licenseProfileId?: number;
    workId?: number;
    typeOfLicense?: string;
    price?: number;
    description?: string,
    uploadedBy?: string,
    fingerprint?: string,
    birthTime?: number,
    activatedStatus?: boolean
}